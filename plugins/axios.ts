// import Vue from 'vue'

// declare module 'vue/types/vue' {
//   interface Vue {
//     $axios({ $axios, redirect }: any): string
//   }
// }

// Vue.prototype.$axios.onRequest((config: any) => {
//   console.log(`Making request to ${config.url}`)
// })

// Vue.prototype.$axios.onError((error: any) => {
//   const code = parseInt(error.response && error.response.status)
//   if (code === 400) {
//     console.log('/400')
//   }
// })
